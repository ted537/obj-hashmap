{
  "name": "obj-hashmap",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "pack": "npm run build && cp package.json dist && npm pack ./dist"
  },
  "files": [
    "./*"
  ],
  "main": "./index.js",
  "devDependencies": {
    "@types/jest": "^29.1.2",
    "jest": "^29.1.2",
    "typescript": "^4.8.4"
  }
}
